"use client";

import { craftStyles } from "@/components/craft";
import { cn } from "@/lib/utils";

export const Hero = () => {
   return (
      <section className={cn("relative bg-linear-primary-top-bottom min-h-screen", craftStyles.layout.sectionHero)}>
      </section>
   );
}; 